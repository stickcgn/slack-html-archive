# Slack archive HTML converter

Converts a slack archive folder (with its `YYYY-MM-DD.json`-files) into a HTML page.

# Requirements

- nodejs (v16+)

# Usage

```
node gen-html.js 《source-folder》《outfile》
```